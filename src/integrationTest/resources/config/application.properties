# classpath:/config/ is a later search location than classpath:/, so this
# merges over src/main/resources/application.properties instead of replacing it.
# A second classpath:/application.properties hid /actuator/info and /api-docs.
# Existing PUT ITs exercise ModifyAny (future bot path). Public Cloud Run stays off.
wiki.write.enabled=true
