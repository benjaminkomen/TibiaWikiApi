# Offline wiki data for API regression. Never points jwiki at Fandom.
# WikiClientConfiguration / JwikiArticleRepository are @Profile("!fixtures").
wiki.fixtures.path=${WIKI_FIXTURES_PATH:regression/fixtures}

